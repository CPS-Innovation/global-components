{
  "name": "cps-global-script-redirect",
  "version": "1.0.0",
  "description": "Redirect stub served at the legacy cps-global-components.js URL; the target URL is substituted in at deploy time from each env's REDIRECT_SCRIPT_URL config",
  "main": "dist/cps-global-components.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^6.0.0",
    "@rollup/plugin-typescript": "^12.0.0",
    "rollup": "^4.59.0",
    "tslib": "^2.6.0",
    "typescript": "^5.0.0"
  }
}
