# Generated by build.sh: the two concatenated legacy bundles and the ESM entry the
# web components import. Sources are common/, classic/ and modern/.
dist/
# Regenerated from the JSDoc in common/ by build.sh, but COMMITTED — it is the
# published surface other packages compile against, so a stale one is a review
# comment rather than a mystery.
!types/
