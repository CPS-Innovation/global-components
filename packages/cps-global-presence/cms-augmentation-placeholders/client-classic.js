(function () {
  try {
    if (window.console && window.console.log) {
      window.console.log(
        "Hello, world from cms-augmentation/client-classic.js (placeholder)"
      );
    }
  } catch (e) {
    // a placeholder must never be the thing that breaks the host page
  }
})();
