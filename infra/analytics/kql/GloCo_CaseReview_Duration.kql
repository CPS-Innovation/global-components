GloCo_CaseReview_PerCase
| where ReviewType in ("First", "Subsequent")
| where Status == "Submitted"
| where StartTime > datetime(2026-03-09T12:00:01)
| extend HoursDiff = round(datetime_diff("minute", SubmittedTime, StartTime) / 60.0, 1)
| where HoursDiff > 0
